# Copy to inventory.ini and fill in your VM's details.
#   ansible-playbook provision.yml   # one-time host setup (needs a sudo user)
#   ansible-playbook deploy.yml       # deploy/update the stack
[devoxguard]
prod ansible_host=203.0.113.10 ansible_user=deploy

[devoxguard:vars]
ansible_python_interpreter=/usr/bin/python3
